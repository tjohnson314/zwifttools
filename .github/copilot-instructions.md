# Copilot Instructions

**Data display preference:** Always prefer showing actual/raw data in graphs and outputs, even when values seem physically implausible. Never clamp, floor, or clip data to "realistic" ranges without explicit approval.
